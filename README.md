# AutoBit Frontend Assessment - Implementation

This is a minimal React + Vite + Tailwind implementation that recreates the provided UI screen.

## What’s included
- Responsive React app (Vite)
- Tailwind CSS for styling
- Simple components: Header, Sidebar, MarketCard list
- Ready for deployment (Vercel / Netlify / etc.)

## How to run locally
1. Install dependencies:
```bash
npm install
```
2. Start dev server:
```bash
npm run dev
```
3. Build:
```bash
npm run build
```

## Notes
- This repository is a starter that focuses on pixel-aligned UI and responsiveness.
- Replace placeholder data or hook to real APIs as needed.

